// fallow-ignore-file unused-enum-member

export enum SitemapChangefreqEnum {
  always = "always",
  hourly = "hourly",
  daily = "daily",
  weekly = "weekly",
  monthly = "monthly",
  yearly = "yearly",
  never = "never",
}
